# Repo Export Manifest
- Repo name: AVD-Tools
- Branch: work (current working branch)
- Changes in this session:
  - Added MANIFEST.md and changes.patch for export packaging.
  - Generated repository ZIP per instructions.
- How to commit locally:
  1. Unzip the archive in your repo workspace.
  2. Review changes with `git status` and `git diff`.
  3. Stage files: `git add .`
  4. Commit: `git commit -m "Package AVD-Tools export"`
